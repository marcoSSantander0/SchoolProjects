@model WebApp.Models.Alumno

@{
    ViewData["Title"] = "Eliminar Alumno";
}

<h2 class="text-2xl font-bold text-red-600 mb-4">¿Eliminar Alumno?</h2>

<div class="bg-white p-4 rounded shadow-md mb-6">
    <p><strong>Nombre:</strong> @Model.Nombre</p>
    <p><strong>Correo:</strong> @Model.Correo</p>
</div>

<form asp-action="Delete" method="post">
    <input type="hidden" asp-for="Id" />
    <button type="submit" class="bg-red-600 text-white px-4 py-2 rounded hover:bg-red-700">Eliminar</button>
    <a asp-action="Index" asp-controller="Admin" class="ml-4 text-gray-700 underline">Cancelar</a>
</form>
